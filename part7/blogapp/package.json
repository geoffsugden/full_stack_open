{
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently --kill-others \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build --prefix client && rm server/dist/assets/*.{js,css} && cp -r client/dist server/",
    "start": "NODE_ENV=production npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "eslint-config-prettier": "^10.1.8"
  },
  "allowScripts": {
    "esbuild@0.25.12": true,
    "bcrypt@6.0.0": true
  }
}
